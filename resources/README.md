# Resources Directory

This directory contains resources needed for the cricket object identifier project.

## Structure

- `images/` - Sample images and datasets for training and testing
- `models/` - Trained model files and weights

## Usage

Place your cricket images in the `images/` directory and model files in the `models/` directory.
